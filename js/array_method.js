forEach();
//  const numbers = [1, 2, 3, 4, 5, 6, 7, 8];
//  const for_each = numbers.forEach((item) => {console.log(item*2);  let res;  res += item; console.log(res) }); console.log(for_each);map()const numbers = [1, 2, 3, 4, 5, 6, 7, 8];const doubledNums = numbers.map((number) => {  return number * 2;});console.log(doubledNums);const players = [  { id: "player-1", name: "Mango", timePlayed: 310, points: 54, online: false },  { id: "player-2", name: "Poly", timePlayed: 470, points: 92, online: true },  { id: "player-3", name: "Kiwi", timePlayed: 230, points: 48, online: true },  { id: "player-4", name: "Ajax", timePlayed: 150, points: 71, online: false },  { id: "player-5", name: "Chelsy", timePlayed: 80, points: 48, online: true },];const playerNames = players.map((number) => number.name);console.log(playerNames);const playerId = players.map((number) => number.id);console.log(playerId);const res = players.map(({ name, online }) => ({ name, online }));console.log(res);
